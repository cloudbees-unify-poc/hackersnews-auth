image:
  url: ""
  # repository: cloudbees-days/hackers-auth
  # tag: latest
  pullPolicy: IfNotPresent

hostname: hackers-auth.example.com

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 128Mi

service:
  port: 8080
